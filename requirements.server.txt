# Install CUDA-specific torch/torchaudio separately on the GPU server first.
# Example (CUDA 12.4):
# pip install "torch==2.6.0" "torchaudio==2.6.0" --index-url https://download.pytorch.org/whl/cu124
# Note: torchaudio 2.9 removed legacy audio backend APIs such as list_audio_backends(),
# and current pyannote.audio runtime can fail with it. Use a matching torch/torchaudio pair below 2.9.

huggingface-hub>=0.34,<1.0
qwen-asr>=0.0.6,<0.1
pyannote.audio>=4.0,<5.0
