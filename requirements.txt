# Legacy fallback for environments that are not using `uv`.
# Prefer pyproject.toml + `uv sync` for new setups.
#
# Base runtime
requests>=2.32,<3.0
python-dotenv>=1.0,<2.0
pydantic>=2.7,<3.0
SQLAlchemy>=2.0,<3.0
typer>=0.12,<1.0
pytest>=8.0,<9.0

# Notes
# - torch / transformers / accelerate / pyannote.audio are intentionally not pinned here yet.
# - CUDA wheel choice is deployment-specific and should be finalized on the target GPU server.
