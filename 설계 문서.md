## 0. Purpose
- 이 문서는 Codex 첫 세션에서 리포지토리에 초기 폴더/파일/문서(README/ARCHITECTURE/PROJECT_CONTEXT)와 실행 가능한 CLI 파이프라인 스켈레톤을 생성하기 위한 부트스트랩 계획이다.
- 로컬에서 개발하고, 클라우드 GPU 서버에 GUI 업로드 후 `nohup env CUDA_VISIBLE_DEVICES=1 ...`로 반복 테스트하는 것을 최우선으로 한다.

1) Project Overview
- 프로젝트명(가칭): asr-meeting-pipeline
- 목표: 회의 음성 파일(m4a 등)을 서버에서 처리하여
  - (1) 전사(ASR)
  - (2) 타임스탬프 보정(Forced Align)
  - (3) 화자 분리(Diarization)
  - (4) 병합(화자/타임 포함 전사본)
  - (5) 회의록 생성(요약/결정/액션)
  - (6) txt 파일 생성
  을 자동 생성한다.
- 개발/테스트 흐름: 로컬 바이브코딩 개발 → 서버 GUI로 업로드 → 서버에서 CLI 실행/로그 확인 → 수정 반복

## 2. Scope: MVP / In Scope / Out of Scope + Success Metrics
2.1 MVP (In Scope)
- CLI 배치 실행으로 단일 오디오 파일 처리
- 8단계 파이프라인(전처리→ASR→Align→Diarize→Merge→Summarize→Export) 구현
- 서버 로컬 폴더에 중간/최종 산출물 저장(work/output/logs)
- SQLite로 job 상태/에러/산출물 경로 최소 기록(추후 Postgres로 교체 가능)
- 요약은 서버 vLLM의 `/generate` 엔드포인트(call.py 스펙)로 GPT-OSS-120B 호출

2.2 Out of Scope
- 프론트/업로드 UI 구현
- 실시간 스트리밍 ASR
- 멀티테넌시(사용자/권한/과금)
- 외부 오브젝트 스토리지(S3/MinIO) 연동(이번 MVP 제외)

2.3 Success Metrics
- 3개 테스트 m4a 파일이 모두 `output/<job_id>/meeting_notes.txt`까지 생성
- 실패 시 `meeting_job`에 실패 단계/에러가 기록되고 `logs/<job_id>.log`로 원인 추적 가능
- 전사/화자/타임스탬프/요약(결정/액션)이 사람이 검토하기에 충분한 품질

## 3. User Flow
1) 서버에 `input/*.m4a` 업로드
2) 실행:
   - 단발: `env CUDA_VISIBLE_DEVICES=1 python run_pipeline.py ./input/test1.m4a --meeting-title "주간 제품 회의"`
   - 백그라운드: `nohup env CUDA_VISIBLE_DEVICES=1 python run_pipeline.py ... > logs/nohup_test1.log 2>&1 &`
3) 진행 로그 확인: `logs/<job_id>.log`
4) 결과 확인:
   - `output/<job_id>/meeting_notes.txt`
   - `output/<job_id>/summary.json`
   - `output/<job_id>/transcript_diarized.json`
5) 오류 시 로컬에서 수정 후 서버에 재업로드/재실행

## 4. Requirements + Acceptance Criteria (core only)
4.1 Core Requirements
- R1. 입력 오디오(m4a/webm/mp4/wav 등)를 ffmpeg로 16kHz mono wav로 변환
- R2. ASR(Qwen3-ASR-1.7B) 결과를 JSON으로 저장
- R3. Forced Align(Qwen3-ForcedAligner-0.6B)로 단어/세그먼트 타임스탬프 JSON 저장
- R4. pyannote diarization 결과를 RTTM + JSON 저장
- R5. overlap 정책으로 단어/짧은 구간 단위 화자 배정 후, `[HH:MM:SS] 화자 X:` 포맷으로 병합
- R6. 요약은 vLLM `/generate`로 호출하여 “핵심요약/결정사항/액션아이템”을 JSON으로 생성
- R7. 최종 txt 포맷으로 내보내기
- R8. job 상태/에러를 SQLite에 기록(추후 Postgres로 교체 가능 구조)

4.2 Acceptance Criteria (Given/When/Then)
- Given `input.m4a`와 ffmpeg 설치
  When CLI 실행
  Then `work/<job_id>/audio_16k_mono.wav` 생성 및 다음 단계 진행
- Given 모델이 정상 로드
  When 파이프라인 완료
  Then `output/<job_id>/meeting_notes.txt` + `summary.json` + `transcript_diarized.json` 존재
- Given diarization/align 결과 존재
  When merge 수행
  Then 각 라인이 `[HH:MM:SS] 화자 A: ...` 형태로 출력
- Given vLLM `/generate`가 동작
  When summarize 호출
  Then 응답 JSON의 `generated_text`를 파싱하여 summary.json을 생성

## 5. IPO Design
5.1 Input
- audio_path (필수)
- meeting_title (옵션; 없으면 파일명 기반)
- output_root/work_root/log_root (옵션; 기본은 .env)
- backchannel_mode (옵션: keep|drop|tag; 기본 keep)
- overwrite (옵션; 기본 false → 새 job_id 생성)

5.2 Process (8 steps)
1. preprocess: ffmpeg 변환(16k, mono wav)
2. asr: Qwen3-ASR-1.7B 전사 → asr.json
3. align: ForcedAligner → align.json
4. diarize: pyannote → diarization.rttm/json
5. merge: overlap 정책 적용 → transcript_diarized.json
6. summarize: vLLM `/generate` → summary.json
7. export: txt 렌더링 → meeting_notes.txt
8. registry/db update: exported_file 기록, job DONE

5.3 Output
- 최종: `output/<job_id>/meeting_notes.txt`
- 부가: `summary.json`, `transcript_diarized.json`
- 중간: `work/<job_id>/*` (wav/asr/align/diarization)
- 로그: `logs/<job_id>.log`

## 6. Data Model + API Contract (if applicable)
6.1 Data Model (SQLite; 추후 Postgres 교체 가능)
- meeting_job
  - id (uuid, PK)
  - status (PENDING/RUNNING/FAILED/DONE)
  - current_stage (PREPROCESS/ASR/ALIGN/DIARIZE/MERGE/SUMMARIZE/EXPORT)
  - input_path, meeting_title, language
  - work_dir, output_dir, log_path
  - error_stage, error_message
  - created_at, updated_at
- transcript_segment
  - id (PK), job_id (FK)
  - speaker_label (화자 A/B/…)
  - start_ms, end_ms
  - text
  - words_json (optional)
- summary_result
  - id (PK), job_id (unique FK)
  - summary_json
  - created_at
- exported_file
  - id (PK), job_id (FK)
  - file_type, path, sha256(optional)
  - created_at

6.2 API Contract
- 이번 MVP는 API 서버 없이 CLI만 제공 (API는 ExecPlan 주제로만 남김)

## 7. Tech Stack & Environment (versions) + setup commands + .env.example
7.1 Stack (Primary)
- OS: Linux(Ubuntu 계열 가정)
- Python: 3.10.x
- ffmpeg: 시스템 설치 필요
- libs: torch(+CUDA), transformers, accelerate, pyannote.audio, pydantic, python-dotenv, typer, requests 등
- DB: SQLite (SQLAlchemy 권장; Step4 부트스트랩에서 최소 코드)

7.2 Setup Commands (server)
```bash
# (프로젝트 업로드 후)
cd asr-meeting-pipeline
python3.10 -m venv .venv
source .venv/bin/activate
python -m pip install -U pip wheel
pip install -r requirements.txt

cp .env.example .env
# .env 수정

# 단발 실행
env CUDA_VISIBLE_DEVICES=1 python run_pipeline.py ./input/test1.m4a --meeting-title "주간 제품 회의"

# 백그라운드 실행
nohup env CUDA_VISIBLE_DEVICES=1 python run_pipeline.py ./input/test1.m4a --meeting-title "주간 제품 회의" > ./logs/nohup_test1.log 2>&1 &
````

7.3 .env.example (한글 설명 괄호 포함)

```env
# =========================
# 기본 경로
# =========================
WORK_ROOT=./work  # (중간 산출물 저장 폴더: 변환 wav, asr/align/diarization 결과 등)
OUTPUT_ROOT=./output  # (최종 산출물 저장 폴더: meeting_notes.txt, summary.json 등)
LOG_ROOT=./logs  # (로그 저장 폴더: 단계별 로그 및 nohup 로그)
INPUT_ROOT=./input  # (테스트 입력 파일을 두는 폴더)

# =========================
# 실행 / GPU
# =========================
CUDA_VISIBLE_DEVICES=1  # (사용할 GPU 번호: 서버에서 1번 GPU를 쓰면 1)
DEVICE=cuda  # (추론 디바이스: cuda 또는 cpu)

# =========================
# 모델 지정 (필요 시 변경 가능)
# =========================
ASR_MODEL=Qwen3-ASR-1.7B  # (ASR 전사용 모델 식별자/경로)
ALIGN_MODEL=Qwen3-ForcedAligner-0.6B  # (Forced Aligner 모델 식별자/경로)
DIARIZATION_MODEL=pyannote/speaker-diarization-community-1  # (화자 분리 모델 식별자)

# =========================
# Hugging Face 다운로드/캐시
# =========================
HF_HOME=./.hf_cache  # (HF 모델 캐시 폴더: 프로젝트 내부에 두면 관리 쉬움)
HUGGINGFACE_HUB_TOKEN=  # (필요 시 입력: pyannote 등 모델 다운로드/약관 동의에 필요할 수 있음)

# =========================
# SQLite (MVP)
# =========================
DB_URL=sqlite:///./work/app.sqlite3  # (MVP DB 위치: 나중에 Postgres로 바꾸기 쉬우려면 URL 형태 유지)

# =========================
# vLLM 요약( GPT-OSS-120B ) 호출 설정 - 현재 서버는 /generate 사용
# =========================
SUMMARY_PROVIDER=vllm_generate  # (요약 호출 방식: vllm_generate=/generate)
SUMMARY_BASE_URL=http://127.0.0.1:8000  # (vLLM 서버 주소)
SUMMARY_ENDPOINT_PATH=/generate  # (생성 엔드포인트 경로)
SUMMARY_API_KEY=  # (필요 시 입력: 내부망이면 비워도 되는 경우 많음)

SUMMARY_TEMPERATURE=0.3  # (생성 온도)
SUMMARY_TOP_P=0.9  # (top_p)
SUMMARY_MAX_TOKENS=1000  # (최대 토큰)
SUMMARY_RESPONSE_KEY=generated_text  # (응답 JSON에서 텍스트가 들어있는 키)

# =========================
# 파이프라인 튜닝(초기값, 필요 시 조정)
# =========================
AUDIO_TARGET_SR=16000  # (전처리 목표 샘플레이트)
AUDIO_TARGET_CHANNELS=1  # (전처리 목표 채널: mono=1)
MERGE_AMBIGUOUS_SEC=0.08  # (overlap 애매 판정: top1-top2가 이 값보다 작으면 애매)
MERGE_AMBIGUOUS_RATIO=0.2  # (overlap 애매 판정: (top1-top2)/단어길이 비율 기준)
BACKCHANNEL_MODE=keep  # (추임새 처리 기본값: keep|drop|tag, CLI 옵션으로 override 가능)
```

## 8. Architecture Summary (diagram + module responsibilities)  
    8.1 Diagram
```text
run_pipeline.py (CLI)
  -> Orchestrator
     -> preprocess (ffmpeg)
     -> asr (Qwen3-ASR-1.7B)
     -> align (Qwen3-ForcedAligner-0.6B)
     -> diarize (pyannote)
     -> merge (overlap policy + backchannel)
     -> summarize (vLLM /generate -> generated_text)
     -> export (txt)
     -> DB update + artifact registry
```

8.2 Module Responsibilities

- src/pipeline/orchestrator.py: 상태전이, 단계 실행 순서, 실패 처리(FAILED 기록)
    
- src/stages/*.py: 단계별 입력/출력 계약(파일 경로)만 책임
    
- src/clients/vllm_client.py: `/generate` 호출(타임아웃/에러 처리/파싱)
    
- src/db/*: SQLite 모델+repo (Postgres 교체 대비)
    
- src/prompts/summary_ko.txt: 요약 프롬프트 템플릿
    
- src/utils/*: 로깅/경로/예외/ffmpeg 래퍼
    
## 9 . Bootstrap File/Folder Structure  
    9.1 Target folder tree

```text
asr-meeting-pipeline/
  run_pipeline.py
  requirements.txt
  .env.example
  PROJECT_CONTEXT.md
  README.md
  ARCHITECTURE.md
  input/
  work/
  output/
  logs/
  src/
    pipeline/
      orchestrator.py
      job_context.py
    stages/
      preprocess.py
      asr.py
      align.py
      diarize.py
      merge.py
      summarize.py
      export.py
    clients/
      vllm_client.py
    db/
      base.py
      models.py
      repo.py
    schemas/
      asr.py
      align.py
      diarization.py
      transcript.py
      summary.py
    prompts/
      summary_ko.txt
    utils/
      logging.py
      paths.py
      ffmpeg.py
      errors.py
      time.py
  tests/
    test_preprocess.py
    test_merge_policy.py
    test_vllm_client.py
```

9.2 Files to generate in first session (1-line purpose each)

- run_pipeline.py: CLI 엔트리포인트(입력/옵션 파싱, orchestrator 호출)
    
- src/pipeline/orchestrator.py: 단계 실행/상태전이/에러 처리
    
- src/stages/*.py: 각 단계 구현 스켈레톤 + I/O 계약
    
- src/clients/vllm_client.py: vLLM `/generate` 호출 래퍼(call.py 호환)
    
- src/db/*: SQLite 스키마/Repo
    
- src/prompts/summary_ko.txt: 요약 프롬프트(메타 + diarized transcript → JSON 요구)
    
- .env.example: 한글 주석 포함 환경변수 템플릿
    
- PROJECT_CONTEXT.md/README.md/ARCHITECTURE.md: 문서팩 초안
    
- tests/*: 최소 핵심 단위 테스트

## 10. Documentation Pack (initial drafts)
    

### /PROJECT_CONTEXT.md

```markdown
# PROJECT_CONTEXT — asr-meeting-pipeline
## 1. 한 줄 요약
서버에서 회의 음성 파일을 전사/화자분리/요약하여 txt 회의록을 생성하는 CLI 파이프라인.

## 2. MVP 목표
- m4a 등 입력을 ffmpeg로 16k mono wav로 변환
- Qwen3-ASR-1.7B 전사, Qwen3-ForcedAligner 타임 보정, pyannote diarization 수행
- overlap 정책으로 화자 포함 전사본 생성
- GPT-OSS-120B(vLLM /generate)로 요약/결정/액션 생성
- output/<job_id>/meeting_notes.txt 생성
- 실패 단계/에러를 SQLite와 로그로 추적 가능

## 3. 현재 결정된 사항(Decision Log)
1) 8단계 파이프라인 구조 유지(전처리→ASR→Align→Diarize→Merge→Summarize→Export)
2) 서버 직접 설치(venv+pips) + nohup 실행, GPU는 CUDA_VISIBLE_DEVICES=1 사용
3) 코드 배포는 클라우드 GPU GUI 업로드(데이터셋 관리)로 수동 반영
4) 산출물은 서버 로컬 work/output/logs 폴더에 저장
5) DB는 SQLite로 시작(향후 Postgres로 교체 가능하도록 repo 레이어 분리)
6) 요약 호출은 vLLM /generate (prompt/temperature/top_p/max_tokens) + generated_text 파싱
7) backchannel(추임새) 정책은 옵션 제공(keep|drop|tag), 기본 keep
8) 액션아이템 담당자/기한이 없으면 "미정"으로 채움
9) overlap 애매 기준: 0.08초 또는 0.2 비율 기준(초기값)

## 4. 현재 상태 요약
- 설계/요구사항/환경 확정 완료
- Codex로 초기 리포/스켈레톤 생성 단계

## 5. 다음 작업(Next Actions)
- Codex로 폴더/파일/문서팩 생성
- 서버에서 preprocess 단독 실행으로 ffmpeg 검증
- vLLM /generate 호출 단독 검증
- 샘플 3개 파일 end-to-end 실행 및 병합/요약 품질 튜닝

## 6. 중요한 제약/주의사항
- 서버 환경은 클라우드 GPU(초기엔 수동 업로드/수동 실행)
- 민감정보/토큰은 로그에 출력 금지
- 전사 텍스트 전체를 로그에 남기지 말 것(요약/일부만)

## 7. 미결 사항(Open Questions)
- diarization 라벨을 화자 A/B/C로 매핑하는 기준(등장순 고정 vs 클러스터 기준)
- 긴 회의(토큰 초과) 시 summarize 입력 분할 전략
- Postgres 전환 시 마이그레이션 도구(Alembic 등) 사용 여부
```

### /README.md

````markdown
# asr-meeting-pipeline
## 1. 한 줄 요약
회의 음성 파일을 전사/화자분리/요약하여 txt 회의록을 생성하는 서버 실행용 CLI 파이프라인.

## 2. 프로젝트 목적
서버(GPU)에서 회의 녹음 파일을 처리해 “화자 포함 전사 + 요약/결정/액션”을 자동 생성한다.

## 3. 대상 사용자
- 로컬에서 개발 후, 클라우드 GPU 서버에 업로드하여 반복 테스트하는 개발자/운영자

## 4. 핵심 기능
- ffmpeg 전처리(16kHz mono wav)
- ASR(Qwen3-ASR-1.7B)
- Forced Align(Qwen3-ForcedAligner-0.6B)
- 화자 분리(pyannote speaker-diarization-community-1)
- overlap 정책으로 화자/타임 병합 전사 생성
- GPT-OSS-120B(vLLM /generate)로 요약/결정/액션 생성
- txt 파일 출력

## 5. 기술 스택
- Python 3.10, torch(+CUDA), transformers, pyannote.audio
- SQLite(추후 Postgres 교체 가능)
- vLLM 서버 호출(/generate)

## 6. 설치 및 실행 방법
### 6.1 요구사항
- Linux 서버
- Python 3.10.x
- ffmpeg 설치
- NVIDIA GPU + CUDA 환경

### 6.2 설치

python3.10 -m venv .venv
source .venv/bin/activate
pip install -U pip wheel
pip install -r requirements.txt


### 6.3 환경 변수(.env)

```bash
cp .env.example .env
# .env를 열어 vLLM 주소/토큰 및 경로를 설정
```

### 6.4 실행

```bash
env CUDA_VISIBLE_DEVICES=1 python run_pipeline.py ./input/test1.m4a --meeting-title "주간 제품 회의"
nohup env CUDA_VISIBLE_DEVICES=1 python run_pipeline.py ./input/test1.m4a --meeting-title "주간 제품 회의" > ./logs/nohup_test1.log 2>&1 &
```

### 6.5 테스트


pytest -q


## 7. 폴더 구조 요약

- input/: 테스트 입력 파일
    
- work/: 중간 산출물
    
- output/: 최종 산출물
    
- logs/: 로그
    

## 8. 사용 방법

- input에 파일을 두고 run_pipeline.py로 실행
    
- output/<job_id>/meeting_notes.txt 확인
    

## 9. 문제 해결

- ffmpeg 변환 실패: ffmpeg 설치 여부 확인
    
- CUDA/GPU 에러: CUDA_VISIBLE_DEVICES 및 nvidia-smi 확인
    
- pyannote 다운로드 실패: HUGGINGFACE_HUB_TOKEN 및 약관 동의 확인
    
- vLLM 실패: SUMMARY_BASE_URL/ENDPOINT_PATH 확인, curl로 /generate 테스트
    

## 10. 참고/링크

- (TBD) 모델/서빙 문서 링크
    

````

### /ARCHITECTURE.md
```markdown
# ARCHITECTURE — asr-meeting-pipeline
## 1. 설계 목표(Goals) / 비목표(Non-goals)
Goals
- 서버에서 CLI 한 번으로 end-to-end 처리
- 단계별 산출물 저장으로 디버깅 용이
- 요약(vLLM) 호출 방식 변경에도 파이프라인 영향 최소화(클라이언트 분리)
- SQLite → Postgres 교체 가능성 확보(repo 레이어)

Non-goals
- 프론트/UI 구현
- 실시간 스트리밍 처리
- 멀티테넌시/권한/과금

## 2. 전체 구조 개요
- run_pipeline.py(엔트리) → orchestrator → stages(preprocess/asr/align/diarize/merge/summarize/export)
- clients/vllm_client.py: vLLM /generate 호출(현재 서버 스펙)
- db/repo.py: 상태/산출물 기록(향후 DB 교체 지점)

## 3. 컴포넌트 책임 분리(역할)
- CLI: 인자 파싱, job 생성, 실행 호출
- Orchestrator: 상태전이, 예외 처리, 단계 실행 순서
- Stages: 각 단계의 입력/출력 파일 계약을 유지
- DB: meeting_job 등 최소 메타 기록
- Prompts: 요약 프롬프트 템플릿

## 4. 데이터 흐름(주요 시나리오)
1) input.m4a 업로드
2) preprocess → work/<job_id>/audio_16k_mono.wav
3) asr → work/<job_id>/asr.json
4) align → work/<job_id>/align.json
5) diarize → work/<job_id>/diarization.rttm + diarization.json
6) merge → output/<job_id>/transcript_diarized.json
7) summarize(vLLM /generate) → output/<job_id>/summary.json
8) export → output/<job_id>/meeting_notes.txt

## 5. API 설계(해당 시)
- MVP는 CLI만 제공. 추후 API 도입 시 orchestrator를 재사용하고 job_id 기반 조회 제공.

## 6. 데이터 모델(해당 시)
- SQLite: meeting_job, transcript_segment, summary_result, exported_file
- repo 레이어로 DB 교체 가능성 확보

## 7. 폴더/파일 구조
(상세 트리는 Bootstrap Plan의 9.1을 따른다)

## 8. 핵심 설계 결정(왜 이렇게 했는가)
- 서버 반복 테스트 속도를 위해 단일 CLI 파이프라인으로 단순화
- stage 경계를 분명히 해서 향후 워커/큐 구조로 확장 가능
- vLLM 호출은 /generate 스펙에 맞추되 클라이언트로 격리

## 9. 에러 처리 & 로깅 규칙
- 실패는 StageError로 래핑하여 job에 error_stage/error_message 기록
- logs/<job_id>.log에 단계별 시작/종료/시간/요약 에러 기록
- 토큰/키 및 전사 전체 텍스트를 로그에 남기지 않음

## 10. 테스트 전략
- preprocess, vllm_client, merge policy, export format, orchestrator 상태전이 최소 테스트
- 샘플 3개 파일로 e2e 수동 검증

## 11. 배포/운영(선택)
- 초기: nohup + 로그 파일
- 안정화 후: systemd 서비스로 전환 가능
````

## 11. 그 외 고려사항

1. Decision Log + Assumptions + Open Questions  
    13.1 Decision Log
    
2. 8단계 파이프라인 유지(큰 구조 변경 없음) — 단계별 책임 분리
    
3. 서버 직접 설치 + nohup 실행 — 클라우드 GPU 환경에 적합
    
4. CUDA_VISIBLE_DEVICES=1 고정 — GPU 혼선 방지
    
5. GUI 업로드로 배포 — git/scp 없이 반복 테스트
    
6. 산출물 서버 로컬 저장(work/output/logs) — 디버깅 우선
    
7. DB는 SQLite(MVP) — 가볍고 추적 쉬움(향후 Postgres 교체 가능)
    
8. 요약은 vLLM `/generate` + `generated_text` 파싱 — 현재 서버 call.py 스펙 호환
    
9. backchannel 옵션 제공(keep|drop|tag) — 나중에 정책 확정/고정 가능
    
10. 담당자/기한 없으면 "미정" — 추정 리스크 회피
    
11. overlap 애매 기준(0.08s or 0.2 ratio) — 일관된 병합 위해 정량화
    
12. Assumptions

	- 서버에서 HF 모델 다운로드 가능(인터넷 가능)
    
	- pyannote 모델 접근을 위한 토큰/약관 동의가 필요할 수 있음
    
	- diarization 라벨은 등장 순서대로 화자 A/B/C 매핑(한 job 내 고정)
    
13 Open Questions (max 5)

1. 긴 회의에서 summarize 입력이 길어질 때 분할 전략(청크 요약→최종 요약) 적용 시점
    
2. diarization 스무딩(최소 구간 길이/병합 기준) 초기값
    
3. Postgres 전환 시 마이그레이션 도구(Alembic) 도입 여부
    
4. overlap 처리에서 “동시 추임새” 사전 목록 확정 여부
    
5. 전사 품질 평가 지표(간단 체크리스트) 도입 여부